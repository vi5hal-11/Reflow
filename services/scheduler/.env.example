# Gemini free tier — create a key at https://aistudio.google.com (no card).
# Powers /parse (capture enrichment) and /reflect (end-of-day insight).
# Unset = both endpoints return 503 and the app degrades gracefully.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
